import os
from setuptools import setup, find_packages

setup(
    name='mcdemo',
    version='0.0.1',
    packages=find_packages('mcdemo'),
    description='Utilities for the Monte Carlo demo',
)
